import { AuthSection, Hero } from "@/components";
import React from "react";

const page = () => {
  return (
    <main>
      <Hero />
      <AuthSection />
    </main>
  );
};

export default page;
